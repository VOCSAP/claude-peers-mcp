node_modules/
out/
dist/
*.log
.DS_Store
# App-written runtime state when dogfooding the app from this dir.
.claude/
# Local, machine-specific node-gyp pin (e.g. msvs_version on Windows). Not shared.
.npmrc
