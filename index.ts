/**
 * claude-peers
 *
 * Peer discovery and messaging for Claude Code instances.
 *
 * Entry points:
 *   - server.ts  -- MCP server (one per session). Connects to the broker via loopback or HTTP.
 *   - broker.ts  -- Shared broker daemon (HTTP + SQLite + WebSocket push).
 *
 * See README.md for setup and usage.
 */

export { }; // module marker
